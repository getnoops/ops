Home:
  Path: ~/.config/no_ops

Log:
  Level: debug

Api:
  GraphQL: http://localhost:8200/api/graphql

Auth: 
  Issuer: http://localhost:8080
  ClientId: ops
  Scopes: 
    - openid
    - profile
    - email
    - offline_access