Home:
  Path: ~/.config/no_ops

Log:
  Level: debug

Server:
  Url: http://localhost:8200

Auth: 
  Issuer: http://localhost:8080
  ClientId: ops
  Scopes: 
    - openid
    - profile
    - email
    - offline_access