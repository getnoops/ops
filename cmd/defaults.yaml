Home:
  Path: ~/.config/ops

Log:
  Level: info
  Formatter:
    Format: text

Auth: 
  Issuer: https://account.getnoops.com/
  ClientId: ops
  Scopes: 
    - openid
    - profile
    - email
    - offline_access